#include <stdio.h>

// This file is used to run current task in Eclipse IDE.
// To do this, just copy necessary code from src here.
// The src folder itself thus should be excluded from build path.
int main() {
    // Sample code to test IDE debugger
    printf("Hello, world!\n");
    int a = 5;
    a++;
    int b;
    scanf("%d", &b);
    printf("%d + %d = %d", a, b, a+b);
}
